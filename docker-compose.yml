services:
  backend:
    build: ./backend
    container_name: workmate_private_backend
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: postgresql://workmate_private:workmate_private@central_postgres:5432/workmate_private
    networks:
      - core_network

networks:
  core_network:
    external: true
